import 'package:flutter/material.dart';

/// LIVE Mode visual tokens extracted from locked design references.
/// Do not scatter hard-coded LIVE status colors in widgets.
class LiveTokens {
  LiveTokens._();

  static const background = Color(0xFF000000);
  static const surface = Color(0xFF121212);
  static const elevated = Color(0xFF1A1A1A);

  static const bronze = Color(0xFFD4AF37);
  static const bronzeSoft = Color(0xFFE5C16F);

  static const liveEvent = Color(0xFFFF4D4D);
  static const liveEventSoft = Color(0xFFFF7A59);

  static const foodTruck = Color(0xFF3DDC97);
  static const market = Color(0xFFB388FF);
  static const happyHour = Color(0xFFFFB74D);
  static const hereNow = Color(0xFF69F0AE);

  static const cardRadius = 14.0;
  static const pillRadius = 999.0;
  static const cardWidth = 168.0;
  static const cardHeight = 220.0;
}
