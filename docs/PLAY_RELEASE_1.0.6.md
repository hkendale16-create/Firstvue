# FirstVue 1.0.6 (versionCode 7) — Play Internal

Package: `com.FirstVue`  
versionName: `1.0.6`  
versionCode: `7`

## What’s new

- Settings search, Create group / community tiles, and a scrollable business-owner start screen
- Home post photos load from the stored object (and sibling buckets)
- Any typed US city is accepted; professional profiles save with city/state only
- Clearer profile-save errors

## Upload

Play Console → FirstVue → Test and release → Internal testing → Create new release → upload the signed `.aab` → Start rollout.

Signed with the existing FirstVue upload key (SHA-1 `49:D7:C2:C3:…:A6:02`).
