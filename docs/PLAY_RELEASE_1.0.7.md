# FirstVue 1.0.7 (versionCode 8) — Play Internal

Package: `com.FirstVue`  
versionName: `1.0.7`  
versionCode: `8`

## What’s new

- Business Tools is Claim / Add / Rental gold tabs (no more squeezed one-letter column on iPhone Safari)
- Settings search, Create group / community tiles, scrollable business-owner start
- Home post photos load from the stored object
- Typed US cities accepted; professional profiles save with city/state only

## Upload

Play Console → FirstVue → Test and release → Internal testing → Create new release → upload the signed `.aab` → Start rollout.

Signed with the existing FirstVue upload key (SHA-1 `49:D7:C2:C3:…:A6:02`).
